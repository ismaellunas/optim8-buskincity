import { config as button } from './button';
import { config as card } from './card';
import { config as cardText } from './card-text';
import { config as carousel } from './carousel';
import { config as columns } from './columns';
import { config as faq } from './faq';
import { config as heading } from './heading';
import { config as icon } from './icon';
import { config as image } from './image';
import { config as latestPost } from './latest-post';
import { config as tabs } from './tabs';
import { config as text } from './text';
import { config as userList } from './user-list';

export default {
    button: button,
    card: card,
    cardText: cardText,
    carousel: carousel,
    columns: columns,
    faq: faq,
    heading: heading,
    icon: icon,
    image: image,
    latestPost: latestPost,
    tabs: tabs,
    text: text,
    userList: userList,
};

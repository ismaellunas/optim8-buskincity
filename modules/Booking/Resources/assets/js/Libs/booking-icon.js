import { iconFormatter } from '@/Libs/icon-class';

export const calendar = iconFormatter('fa-calendar');
export const duration = iconFormatter('fa-hourglass-start');
export const timezone = iconFormatter('fa-earth-asia');

export default {
    calendar,
    duration,
    timezone,
};
